import React from 'react';
declare type Props = {
    name: string;
    onClick: () => void;
    styles?: {
        root?: string;
    };
};
declare const IconButton: React.FC<Props>;
export default IconButton;
