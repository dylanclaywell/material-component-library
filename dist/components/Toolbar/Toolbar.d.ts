import React from 'react';
declare type Props = {
    children?: React.ReactNode;
    styles?: {
        root?: string;
    };
};
declare const Toolbar: ({ children, styles }: Props) => JSX.Element;
export default Toolbar;
